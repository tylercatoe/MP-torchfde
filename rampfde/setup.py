"""
Minimal setup.py for backward compatibility.

This package uses pyproject.toml for modern configuration.
This setup.py is provided for compatibility with older pip/setuptools versions.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()