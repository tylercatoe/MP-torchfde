# Performance Tests

This folder is reserved for performance/regression benchmarks, mirroring the
`rampde/tests/performance` layout.

No performance tests are included yet; add them here as the mixed-precision
fractional adjoint implementation matures.
